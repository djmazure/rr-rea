board 'zybo' matches zybo-classic-1, zybo-classic-2, zybo-z7-20; using zybo-classic-1: --cable 210279539777 is its JTAG cable.
💡 cable confinement is installed but not active for this lease — run `rr lease confine setup` (then re-acquire the lease to confine it). 
Until then the board is advisory-locked only.
✓ bit-sha : MATCH — PL is running the expected bit (0x79a5af86)
    expected: 0x79a5af86 (sidecar:zybo_classic_rea_source.usr_access)
    device:   0x79a5af86
    outcome:  CONFIRMED (vendor=xilinx mechanism=usr-access-32)
